# geolint

Fast, zero-dependency GPS **point validation** + **path distance** utilities.

- ✅ Bounds, accuracy, timestamp, short-step, speed/jump checks
- ✅ Batch accumulation (`validateBatch`)
- ✅ Path distance in meters (`pathDistanceM`)

## Install

```bash
npm i geolint
```
